{
  "$schema": "accepted override drift - see scripts/ci/check-override-advisories.mjs",
  "$comment": [
    "Override pins that are known to be vulnerable and are accepted for now.",
    "An entry is matched on package + pinned + advisory, so it expires by itself:",
    "raise the override, or let a new advisory land against the same pin, and the",
    "check goes red again. Adding an entry is a deliberate decision that needs a reason."
  ],
  "accepted": [
    {
      "package": "brace-expansion",
      "pinned": "1.1.16",
      "advisory": "GHSA-mh99-v99m-4gvg",
      "severity": "high",
      "fixedIn": "5.0.8",
      "reason": "1.1.16 is the highest 1.x release and it clears GHSA-3jxr-9vmj-r5cp, but this advisory covers <=5.0.7 across every major line and is only patched in 5.0.8. brace-expansion 5.x is not drop-in for the 1.x line: 1.x is 'module.exports = expandTop' while 5.x exports a named 'expand', and the consumer of this pin (minimatch@3.1.4) does 'var expand = require(\"brace-expansion\")' and calls it, so forcing 5.0.8 here throws at runtime. Same class of change as the @hono/node-server major - needs its own compatibility-tested PR. The separate 'brace-expansion@>=5.0.0' override was raised to 5.0.8 and is clean."
    },
    {
      "package": "@hono/node-server",
      "pinned": "1.19.13",
      "advisory": "GHSA-frvp-7c67-39w9",
      "severity": "moderate",
      "fixedIn": "2.0.5",
      "reason": "Pre-existing override drift, found by the first run of this check (2026-07-25). Fixing it is a 1.x -> 2.x major bump that needs compatibility testing, so it is tracked separately rather than folded into the advisory-bump PR."
    }
  ]
}
